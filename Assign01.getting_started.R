#Ex 61cf39b0369e7f0d08196c16
#Variable assignments and computations

#Define a variable named `radius` and assign the value of 3 to it
#Complete implementation...
radius <- 3

#Compute the area of a circle with radius 3 and assign it to a 
#variable `area`. The value of Pi can be retrieved using the `pi` variable
#Complete implementation...
area <- pi * radius ^2

#this should print the value of the area
print(area)

#--------------------------

#Using functions

#Obtain the data type of the `area` and assign it to a variable `type_of_area`
#Complete implementation...
type_of_area <- class(area)

#this should print the data type of area
print(type_of_area)

#Use the `round()` function and round of the area to 1 decimal place.
#Assign the result to area1
#Complete implementation...
area1 <- round(area, digits = 1)

#this should print value of area rounded to 1 decimal place
print(area1)

#--------------------------
